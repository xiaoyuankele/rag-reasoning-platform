# 前端对话工作区

> 当前状态：F0、F1 和 F3 基础关键词检索已经完成；P6 前端第一批认证 API、Auth Store、公共认证页面与
> 受保护应用外壳也已实现。真实 Go 后端、PostgreSQL、Mailpit、Vite `/api` 代理和浏览器表单纵向联调
> 已经通过。F2-A 文档分页列表、单文件上传和后端精确内容去重结果提示已经实现；F2-B 详情、解析任务轮询、
> chunks 分页和删除二次确认也已完成，真实上传、解析和删除已由用户验证。批量导入解析队列现已完成代码、
> 自动化门禁和只读页面验收。F2-C 第一阶段“单篇 / 全部文档”共享检索范围选择器也已完成，关键词检索不再
> 手填文档 ID。上传前重复预检也已按正式契约接入，使用单路 Web Worker 分块计算 SHA-256；完整门禁及简单真实
> 批次验收通过，33 份选择中 32 次重复正文上传被跳过。固定样本性能基准、大文件、故障降级、chunks 专项和
> 双用户产品隔离仍待执行。2026-08-21 已与新的高性能、高并发工程目标对齐：前端继续保持哈希并发 1、上传并发 2
> 和有界集中轮询；后端 Worker/Process Pool 配置不映射到浏览器，429/503 退避仍是待开发能力。独立文档向量化页面
> 已于 2026-08-21 完成基础切片：单篇/批量申请、逐项结果、集中轮询、等待/排队取消、页面隐藏暂停轮询和本浏览器
> 会话恢复均已接入；随后已接入 `POST /embedding-jobs/latest`，刷新和换设备可批量发现最近任务，只对活动状态继续轮询。
> F3 同一 chunk 多关键词 all/any 检索及多词高亮已经完成；F4 带来源问答基础页也已实现，当前远程问答开关保持关闭。
> 前后端最新提交已通过 `9dea1d4` 合入统一分支；当前 HEAD `d7c9a52` 已进一步纳入 Embedding/Answer 容量控制，
> 本地与远程一致，尚未合入 `main`。前端下一切片是按正式 code 和 `Retry-After` 展示可恢复的容量状态。

## 新对话第一条任务建议

先读取：

1. `../共享/协作协议/开发协作协议.md`；
2. `../../docs/shared/api/http-api-overview.md`；
3. `../../docs/frontend/architecture/frontend-roadmap.md`；
4. 本文件和 `规范/前端分层开发规范.md`。

当前还应读取 `../共享/前后端接口契约/P6个人用户认证前端对话交接.md`。
开始 F4 前还要读取 `../共享/前后端接口契约/P6前端开发前待后端确认事项.md`。

当前下一步先评审统一分支并合入 `main`；随后对独立向量化页面完成真实后端纵向验收，重点覆盖
waiting → queued → processing → succeeded/failed、取消竞态和当前 document revision 的 AI-ready 事实。用户明确开启
`ANSWER_ENABLED` 后，再验收真实回答、来源、Token、超时和费用。上传预检仍需固定样本 A/B、大文件、网络/5xx fail-open、
400/401/413 阻断和双用户隔离专项；跨模块 429/503 退避仍是后续性能切片。

## 当前前端专项分工

- 用户当前参与架构、核心逻辑、产品流程和设计方向确认，不承担代码书写；
- Codex 在用户明确授权实现后负责前端骨架、业务代码、测试、文档同步和验证；
- 开始每段工作前仍需按共享协议说明当前工作、目的、路线位置、输入输出、完成标准和非目标；
- 架构选择明显影响后续成本时先讨论，未授权时不因模板已存在就继续编写业务代码。

## 已确认方向

- Vue 3 Composition API + TypeScript + Vite；
- Vue Router、轻量 Pinia、Axios API 层；
- Element Plus 选择性使用，页面视觉保持简约、类 Codex、内容优先；
- 轻量 Feature-first 模块化；
- Vitest + Vue Test Utils，Playwright 后置到 F5；
- ESLint + Prettier 作为工程规范；
- 当前不采用 Nuxt、SSR、微前端、GraphQL 或复杂设计系统。

## 独立记录

- 每个开发日收尾固定形成两份记录：`复盘/` 的“学习与架构复盘”和 `进度/` 的“开发进度与计划对照”；
- 学习与架构复盘记录学到的知识、完整数据流、分层责任、设计取舍、测试认识和仍需练习的内容；
- 开发进度与计划对照记录项目当前里程碑、理想目标、当日变化、验证证据、剩余差距和下一步；
- 用户在对话中提出可长期复用的前端知识或架构问题时，及时把问题和回答追加到 `知识库/问题.md`；
- 真实 Bug、联调异常、契约缺口和文档不一致才进入 `问题管理/`，不把普通学习提问登记为 Bug；
- 可用性、页面性能和端到端验收资料写入 `评估/`。

后端历史不会复制进来；跨端信息通过正式 API 契约连接。
