# 前端调查处置档案

每个问题按以下模板追加，不删除历史：

```markdown
## 问题编号与标题

- 发现时间：
- 发现环境：
- 用户可见现象：
- 最小复现步骤：
- 期望结果：
- 实际结果：
- 证据：
- 影响范围：
- 根因：
- 候选方案与取舍：
- 确认方案：
- 修改内容：
- 回归结果：
- 当前状态：
```

## ENV-FE-001：PowerShell 禁止运行 `npm.ps1`

- 发现时间：2026-08-14
- 发现环境：Windows PowerShell；Node.js 安装在 `D:\software\node`
- 用户可见现象：`node --version` 正常，但 `npm --version` 返回 `PSSecurityException`
- 最小复现步骤：在 PowerShell 中执行 `npm --version`
- 期望结果：显示 npm 版本
- 实际结果：PowerShell 优先解析 `D:\software\node\npm.ps1` 并因执行策略拒绝运行
- 证据：`Get-Command npm -All` 同时发现 `npm.ps1`、`npm.cmd` 和无扩展名入口
- 影响范围：只影响 PowerShell 中的命令解析；Node.js 和 npm 安装本身正常
- 根因：命令解析优先选择 PowerShell 脚本，而当前环境不允许执行该 `.ps1`
- 候选方案与取舍：修改 ExecutionPolicy 可恢复 `npm` 简写，但会改变本机安全策略；直接使用
  `npm.cmd` 不改变策略且满足开发需要
- 确认方案：使用 `npm.cmd` 和 `npx.cmd`
- 修改内容：未修改系统策略和项目代码
- 回归结果：`npm.cmd --version` 返回 `11.16.0`；Vite 模板依赖安装、审计和开发服务器启动成功
- 当前状态：已验证

## CONTRACT-P6-001：Origin / CSRF 声明与实现待核对

- 发现时间：2026-08-17
- 发现环境：前端准备接入 P6 Cookie 认证时，对照正式文档与后端路由装配
- 用户可见现象：当前同源 Vite 代理可以完成认证，但无法据此证明生产环境改变状态请求已经具备 Origin/CSRF 门禁
- 最小复现步骤：对照 `docs/shared/api/http-api-overview.md` 的安全声明，检查后端 Router 和 Server 中间件装配
- 期望结果：正式声明、稳定错误码、路由中间件和自动化测试保持一致
- 实际结果：正式文档声明已校验，但前端只读核查没有在当前路由层找到对应实现
- 证据：`chatgpt/共享/前后端接口契约/P6前端开发前待后端确认事项.md#2-contract-p6-001同源-origin--csrf-声明与当前代码待核对`
- 影响范围：不阻塞本地同源认证和 F2 基础开发；阻塞“P6 已完成全部生产安全门禁”的最终声明
- 根因：尚待后端核查，当前不能预判是实现遗漏还是文档状态超前
- 候选方案与取舍：后端补齐中间件/错误码/测试，或把正式文档修改为准确的计划状态
- 确认方案：等待后端回交；前端不删除或伪造浏览器 Origin
- 修改内容：已经建立跨端确认文档，尚未修改生产代码
- 回归结果：待后端实现或文档结论冻结后执行
- 当前状态：待讨论

## FLOW-P4-001：新文档向量就绪流程未冻结

- 发现时间：2026-08-17
- 发现环境：规划 F2 文档管理和 F4 语义检索/问答衔接
- 用户可见现象：新文档解析完成后可能只能关键词检索；用户缺少明确方式进入 AI-ready
- 最小复现步骤：对照手动向量任务接口、语义检索 `409` 门禁和“首版 UI 不直接暴露”声明
- 期望结果：冻结从文档解析 ready 到当前模型向量完整的用户流程，并能在刷新后恢复状态
- 实际结果：单篇/批量创建、按任务 ID 查询和等待/排队取消契约已冻结；前端独立向量化页面已接入。
  但后端没有按 document 查询最新任务或分页列出任务的接口，前端无法在换设备或清理会话后发现历史状态
- 证据：`chatgpt/共享/前后端接口契约/P6前端开发前待后端确认事项.md#3-flow-p4-001新文档如何进入向量就绪态尚未形成产品闭环`
- 影响范围：不阻塞 F2 列表、上传、解析、chunks 和删除；阻塞 F4 对新文档的完整产品验收
- 根因：任务命令和按 ID 查询已经形成契约，但文档到最新任务的发现契约仍缺失
- 候选方案与取舍：用户显式触发、解析成功后自动触发，或后台维护触发；三种方案的费用透明度和恢复能力不同
- 确认方案：第一版建立独立 `/embeddings` 页面，按明确用户选择创建任务；只用 Session Storage 保存当前浏览器会话
  的 document ID → job ID 映射，页面明确提示“未跟踪不等于从未向量化”。后续正式发现接口取代该临时恢复能力
- 修改内容：新增独立 embedding entity、API、composable、UI 和路由；实现单篇/批量申请、逐项失败、集中有界轮询、
  waiting/queued 取消、页面隐藏暂停轮询和已知 job ID 刷新恢复
- 回归结果：API/状态机/组件自动化通过；类型、Lint、格式、构建和真实空文档页面只读检查通过；真实模型纵向验收待执行
- 当前状态：待讨论

## DOC-P6-001：密码重置范围描述不一致

- 发现时间：2026-08-17
- 发现环境：读取 P6 正式架构、HTTP 契约和后端交接文档
- 用户可见现象：新对话可能根据旧段落误判“找回密码”仍是 P6 非目标
- 最小复现步骤：对照个人用户域前部非目标描述与已经实现的密码重置接口、代码和测试
- 期望结果：正式架构文档与已经交付的能力一致
- 实际结果：局部旧描述尚未随功能完成而清理
- 证据：`chatgpt/共享/前后端接口契约/P6前端开发前待后端确认事项.md#4-附带文档清理密码重置范围描述不一致`
- 影响范围：不影响运行代码；影响新对话理解和路线判断
- 根因：功能范围演进后，旧架构段落没有同步更新
- 候选方案与取舍：直接清理正式文档旧描述，不需要修改接口或代码
- 确认方案：后端更新 P6 正式文档时一并修复
- 修改内容：已经登记并加入修复队列
- 回归结果：待核对正式文档不再把密码重置列为非目标
- 当前状态：待修复

## CONTRACT-F2-001：重复文档上传策略未冻结

- 发现时间：2026-08-18
- 发现环境：设计 F2 单文件上传、历史文档列表和上传重试流程
- 用户可见现象：相同内容可以使用相同或不同文件名重复上传，产生两条文档、两份存储和后续重复解析
- 最小复现步骤：连续上传同一文件，或复制并修改文件名后再次上传
- 期望结果：明确同名、同内容、相似内容、网络重试和跨用户重复分别怎样处理
- 实际结果：后端已按 `(owner_user_id, sha256)` 原子创建或返回已有记录；首次上传返回
  `201 + duplicate:false`，同用户重复内容返回 `200 + duplicate:true`，不同用户仍各自创建独立记录
- 证据：`docs/backend/architecture/document-upload-deduplication.md`、
  `docs/shared/api/http-api-overview.md` 和提交 `2224984 feat: deduplicate document uploads per user`
- 影响范围：F2 可以稳定区分新建与重复成功；不再阻塞上传前端开发
- 根因：数据库初版只保存哈希但没有冻结个人用户产品策略，现已由后端应用服务和数据库唯一约束补齐
- 候选方案与取舍：允许重复最简单但浪费存储和计算；前端单独计算哈希只能提示且对 200 MiB 文件成本高；
  后端按 OwnerScope 拒绝精确重复可以并发收口，但需要处理已有重复数据和明确是否允许强制上传
- 确认方案：第一版在同一用户内阻止精确字节重复；文件名不参与判断；相似内容不自动去重；跨用户不共享记录
- 最终契约：重复不是异常，返回已有文档的 `200 + duplicate:true`；前端提示“该内容已存在”并刷新列表
- 并发约束：数据库使用 `(owner_user_id, sha256)` 唯一约束作为最终裁判，前端不承担并发正确性
- 前端策略：上传中禁用重复提交；不按文件名拒绝；同时校验 HTTP 状态和 `duplicate` 字段；重复结果保留原名称与 ID
- 修改内容：建立文档模型、运行时 DTO 校验、分页列表、上传状态和重复提示；同步正式路线与本地记录
- 回归结果：后端并发与真实 HTTP 测试已通过；前端 10 个测试文件 30 个测试、类型、Lint、格式和构建通过；
  真实浏览器重复上传待本地服务环境启动后执行
- 当前状态：已验证

## FLOW-F2-002：上传成功后解析流程未闭环

- 发现时间：2026-08-18
- 发现环境：Docker Go 后端、PostgreSQL、Vite 前端和真实登录 Session 联调
- 用户可见现象：一篇从未上传过的 PDF 显示上传成功，但文档状态一直为“等待解析”
- 最小复现步骤：登录文档页，选择新 PDF 上传，等待并反复刷新文档列表
- 期望结果：用户能够明确启动解析，文档随后进入“解析中”，最终进入 ready 或 failed
- 实际结果：文档 `id=335` 保持 `uploaded`；页面没有解析操作，也没有自动创建任务
- 证据：`documents.id=335` 的状态为 `uploaded`；`document_jobs` 没有对应记录；后端日志只有
  `POST /documents -> 201` 和 `GET /documents -> 200`，没有 `POST /documents/335/process`
- 影响范围：文件与元数据保存正常、内容去重正常，但新文档不能通过当前前端流程进入文本块、关键词检索、
  向量和问答链路
- 根因：后端有意把上传与解析拆成两个接口；F2-A 前端只实现列表与上传，尚未消费
  `POST /documents/:id/process` 和解析任务查询接口
- 候选方案与取舍：首次新建后前端自动创建解析任务最顺畅，但必须处理“上传成功、排队失败”；显式“开始解析”
  按钮最容易理解和重试，但多一步操作；把排队强行并入上传接口会重新耦合两个用例，不作为首选
- 初步建议：保持后端两个接口分离；首次 `201 + duplicate:false` 后由前端用例自动排队，同时为 uploaded/failed
  文档保留显式开始/重试入口，并轮询任务状态。重复上传依据已有文档状态决定提示，不重复创建活动任务
- 确认方案：保持上传与解析接口分离；上传成功自动选中详情，由用户显式点击“开始解析”；uploaded/failed
  保留开始/重试入口，活动任务通过 job ID 轮询，避免上传成功但排队失败时混淆两次操作结果
- 修改内容：F2-B 已接入详情、创建解析任务、任务轮询、文档状态刷新、chunks 和删除保护
- 回归结果：自动化已覆盖创建任务和 queued 到终态后停止轮询；真实 `uploaded -> queued -> processing -> ready/failed`
  仍待使用测试文档纵向验证
- 当前状态：已修复待验证

## CONTRACT-F2B-003：刷新后无法恢复文档活动任务详情

- 发现时间：2026-08-18
- 发现环境：设计 F2-B processing job 刷新恢复状态机时对照后端 Handler 与应用服务
- 用户可见现象：创建解析任务后立即刷新，若任务仍为 queued，文档可能仍显示 uploaded/failed，页面丢失 job ID
- 最小复现步骤：创建解析任务，在 Worker 领取前刷新详情页
- 期望结果：前端能恢复当前文档的活动任务 ID、queued/processing 状态、尝试次数和错误信息
- 实际结果：后端仅提供按 job ID 查询；文档详情不返回活动/最近 job；queued 阶段文档有意不切换 processing
- 证据：`POST /documents/:id/process` 返回 job ID；`GET /processing-jobs/:id` 必须已有 ID；
  `queue_processing.go` 明确 Worker 领取前文档保持 uploaded/failed
- 影响范围：不阻塞当前页面持有 job ID 时的完整轮询；刷新后可通过再次创建收到 409 并轮询文档最终状态，
  但无法显示排队详情，Worker 长时间未领取时用户反馈较弱
- 根因：查询接口以 job 为入口，缺少以 OwnerScope + document ID 查询活动/最近任务的恢复端口
- 候选方案与取舍：详情内嵌 job 会耦合 DTO；新增 `GET /documents/:id/processing-job` 最直接；
  列举全部 jobs 功能更大但当前没有产品需求
- 确认方案：前端当前用 job ID 优先轮询；丢失 ID 时以 409 + 文档详情轮询降级恢复；后端后续评估按文档查询接口
- 修改内容：降级恢复已经实现；正式 F2-B 文档明确该限制，不伪造 job ID
- 回归结果：自动化覆盖已知 job ID 终态停止；queued 刷新场景待接口补齐后做完整状态验收
- 当前状态：待讨论

## UI-F2C-001：空文档库遮住失效范围恢复入口

- 发现时间：2026-08-19
- 发现环境：真实登录态、当前账户没有解析 ready 文档，访问 `/search?document_id=42`
- 用户可见现象：下拉框和说明显示文档 #42 不可用，但没有一键切换到全部文档的按钮
- 最小复现步骤：账户没有 ready 文档；打开带正整数 `document_id` 的检索 URL
- 期望结果：明确说明范围失效，并允许用户主动切回全部文档
- 实际结果：`state === empty` 的 `v-else-if` 先渲染，后面的失效范围 warning 分支没有进入
- 证据：首次真实 DOM 快照只有失效说明与普通 empty 提示，没有“使用全部文档”按钮
- 影响范围：只影响选项为空且 URL 保留旧单篇 ID 的恢复操作；不影响全部范围和搜索 API
- 根因：把选项加载状态和范围有效性放在同一条互斥展示链，empty 错误地覆盖了范围恢复状态
- 候选方案与取舍：静默清除 ID 会扩大检索范围且可能误导用户；调整展示优先级能保留事实和显式选择
- 确认方案：加载/错误与范围有效性分别渲染；empty + unavailable 优先显示失效 warning 和恢复按钮
- 修改内容：收紧 unavailable 只在 success/empty 后判断；拆分 notice 条件；把组件测试改为真正空选项场景
- 回归结果：相关 2 个测试文件 4 个测试通过；真实页面显示“使用全部文档”，点击后 URL 清除 document_id；控制台无错误
- 当前状态：已验证

## UI-F3-001：零命中与连接失败反馈不够清晰

- 发现时间：2026-08-19
- 发现环境：真实登录态，在单篇 ready 文档中使用较长中文词组进行关键词检索
- 用户可见现象：页面返回空结果时，用户判断“检索功能与后端还没连接”
- 最小复现步骤：选择文档 #359，输入正文中未连续出现的“磁悬浮振动”并检索
- 期望结果：页面明确区分“后端成功返回零结果”与网络/服务错误，并解释当前匹配语义
- 实际结果：请求已经到达 `/search` 且返回 `200`，但原空态只写“没有找到”，未直接提供连接成功证据
- 证据：后端日志连续记录 `GET /search -> 200`；文档 #361 使用短词“振动”返回 1 个真实文本块
- 影响范围：不影响请求正确性和结果安全性；影响用户理解、手动验收和关键词调整
- 根因：当前后端是连续字面子串匹配，产品文案没有明确说明多词不会自动拆分
- 确认方案：输入帮助说明连续字面匹配；零命中标题明确“后端检索已完成”，并给出缩短关键词示例
- 修改内容：更新 KeywordSearchPanel 空态与帮助文案，增加零命中/连接失败区分测试并同步架构记录
- 回归结果：自动化 20 个测试文件、64 个测试通过；真实零命中页面显示“后端检索已完成”；随后恢复
  `q=irregularity` 并正常显示 107 个文本块和关键词高亮；控制台无 warning/error
- 当前状态：已验证

## UI-F4-001：文本解析与向量状态混淆且缺少全部文档操作

- 发现时间：2026-08-21
- 发现环境：真实登录态访问 `/embeddings`，检查解析状态筛选和文档行状态
- 用户可见现象：解析筛选没有直观的“未解析”分类；未发现向量任务的文档仍显示“已解析”，容易被理解为已经完成向量化；
  初次修复后向量状态仍只有行内提示，没有形成独立分类；页面只能手动选择至多 100 篇，缺少全部文档向量化入口
- 最小复现步骤：打开向量化页面，观察 ready 文档的状态标签和“当前会话未跟踪”说明；尝试一次选择整个文档库
- 期望结果：文本解析与向量状态明确分栏表达；uploaded 有“文本未解析”分类；能够显式确认并提交全部可操作文档
- 实际结果：数据库核验当前账号 94 篇 ready、2 篇 failed、0 篇 uploaded；94 篇 ready 均存在 chunks，因此不是前端把
  uploaded 映射成 ready，而是“已解析”文案与向量状态靠得太近造成语义误读。原选择器受后端单批 100 项上限约束
- 证据：PostgreSQL 只读聚合与 ready 文档零 chunks 反查；`EmbeddingWorkspacePanel.vue` 原状态文案和
  `use-embedding-workspace.ts` 原单批选择限制
- 影响范围：不影响后端状态正确性；影响用户判断文档是否已经向量化，也阻碍大文档库的一键任务申请
- 根因：页面虽保存两套状态模型，但展示语言没有持续带上“文本/向量”前缀；把后端单批限制直接等同为页面总操作上限
- 候选方案与取舍：仅改标签成本最低但不能解决全库操作；一次发送全部 ID 会违反后端最多 100 项契约；前端顺序拆批可复用
  现有 API 和逐项结果，但必须保留费用确认、部分成功和中途失败事实
- 确认方案：解析标签改为“文本未解析/解析中/已解析/解析失败”，向量提示统一使用“向量状态”；新增全部文档操作，
  对当前可操作文档按每批 100 篇顺序提交，非 ready 文档由后端保存为 waiting_document
- 修改内容：增加文本和当前会话向量两套状态分类、计数、筛选和行级徽标；增加全部文档确认入口；composable 支持任意数量
  ID 的顺序拆批、逐项反馈和部分完成错误摘要；保持文档 feature 与 embedding feature 解耦
- 回归结果：组件覆盖 uploaded 不误显 ready 与全部提交入口；状态机覆盖 201 篇拆为 100/100/1；26 个测试文件、
  90 个测试、类型、Lint、格式和生产构建通过；真实页面显示未解析 0、已解析 94、解析失败 2，全部操作为 96 篇，
  确认框正确说明 1 批和 2 篇非 ready 文档，检查后返回且未提交真实任务；当前会话向量筛选组件回归、26 个测试文件、
  90 个测试、类型、Lint、格式和构建通过；真实页面显示独立向量筛选、未跟踪 96 和行级向量徽标
- 当前状态：已验证
